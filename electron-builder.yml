appId: com.knoux.clipboardai
productName: Knoux Clipboard AI
copyright: "Copyright © 2024 Abu Retaj"

# Build Settings
directories:
  output: release
  buildResources: assets

# ASAR: Packages source code into an encrypted/compressed archive for performance and basic protection
asar: true
asarUnpack:
  - "**/*.node" # Unpack native modules

# Compression levels: 'store' (fastest), 'normal', 'maximum' (smallest file)
compression: maximum

# Files to include in the final package
files:
  - from: "."
    filter:
      - "dist/**/*"
      - "build/**/*"
      - "package.json"
      - "node_modules/**/*"
      - "!node_modules/.cache"
      - "!**/*.ts"
      - "!**/*.tsx"
      - "!**/*.map"
      - "!tests/"
      - "!src/"

# Windows Configuration
win:
  target: 
    - nsis
    - portable
  icon: assets/icons/icon.ico
  requestedExecutionLevel: asInvoker
  signingHashAlgorithms:
    - sha256
  verifyUpdateCodeSignature: false # Set to true if you have a Code Signing Certificate

# Windows Installer (NSIS) Details
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: true
  displayLanguageSelector: true
  installerIcon: assets/icons/icon.ico
  uninstallerIcon: assets/icons/icon.ico
  uninstallDisplayName: "Knoux Clipboard AI (Uninstall)"
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: "Knoux Clipboard AI"
  runAfterFinish: true

# macOS Configuration
mac:
  target:
    - dmg
    - zip
  category: public.app-category.productivity
  icon: assets/icons/icon.icns
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist

# Linux Configuration
linux:
  target:
    - AppImage
    - deb
    - rpm
  category: Utility
  icon: assets/icons/icon.png
  description: "Advanced AI Clipboard Manager"
  synopsis: "Productivity Tool"
  maintainer: "Abu Retaj <support@knoux.com>"

# Auto Update Configuration
publish:
  provider: github
  owner: knoux
  repo: clipboard-ai
  releaseType: release
