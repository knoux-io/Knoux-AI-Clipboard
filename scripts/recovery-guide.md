# 🛡️ دليل استعادة نظام KNOUX Clipboard AI

## 📋 نظرة عامة

يقدم هذا الدليل خطوات استعادة وتصحيح نظام KNOUX Clipboard AI في حالة حدوث مشاكل أو أخطاء. يتضمن إجراءات للتعافي من الفشل في البناء، مشاكل التثبيت، أخطاء التشغيل، وفقدان البيانات.

## 🚨 حالات الطوارئ الشائعة

### 1. فشل البناء
```
❌ الأعراض:
├── أخطاء TypeScript
├── فشل في تحميل التبعيات
├── أخطاء في الذاكرة
├── فشل في عملية البناء
└── ملفات .env مفقودة أو تالفة
```

### 2. مشاكل التثبيت
```
❌ الأعراض:
├── فشل في تثبيت التطبيق
├── أخطاء في الأذونات
├── ملفات نظام مفقودة
├── تعارض مع برامج أخرى
└── فشل في التوقيع
```

### 3. أخطاء التشغيل
```
❌ الأعراض:
├── التطبيق لا يفتح
├── أعطال متكررة
├── فقدان البيانات
├── بطء شديد في الأداء
└── أخطاء في قاعدة البيانات
```

### 4. فقدان البيانات
```
❌ الأعراض:
├── تاريخ الحافظة مفقود
├── إعدادات تمت إعادة تعيينها
├── ملفات تعريف تالفة
├── قاعدة بيانات تالفة
└── أخطاء في التشفير
```

## 🚀 إجراءات الاستعادة السريعة

### الخطوة 1: التشخيص الأولي
```bash
# تشغيل أداة التشخيص
npm run doctor

# فحص النظام
node scripts/doctor.js

# التحقق من الملفات الأساسية
npm run check:integrity
```

### الخطوة 2: السجلات والتحليل
```bash
# عرض سجلات التطبيق
tail -f logs/app.log

# سجلات الأخطاء
tail -f logs/error.log

# سجلات النظام
npm run logs:system
```

### الخطوة 3: الإجراءات التصحيحية
```bash
# الإجراء المناسب حسب الخطأ
npm run fix:<issue-type>

# أو استخدام الأداة العامة
npm run recover
```

## 🔧 استعادة من فشل البناء

### الحالة 1: أخطاء التبعيات
```bash
# الحل الكامل
rm -rf node_modules package-lock.json
npm cache clean --force
npm install

# التحقق من التوافق
npm ls

# تحديث التبعيات
npm update
npm audit fix
```

### الحالة 2: أخطاء TypeScript
```bash
# تنظيف ملفات البناء
npm run clean
rm -rf dist build

# التحقق النوعي
npm run type-check -- --noEmit

# إعادة البناء
npm run build
```

### الحالة 3: مشاكل الذاكرة
```bash
# زيادة حد الذاكرة
export NODE_OPTIONS="--max-old-space-size=4096"

# أو في Windows
set NODE_OPTIONS=--max-old-space-size=4096

# أو في PowerShell
$env:NODE_OPTIONS="--max-old-space-size=4096"
```

### الحالة 4: ملفات .env
```bash
# استعادة من النسخة الاحتياطية
cp .env.example .env

# أو إنشاء جديد
cat > .env << EOF
NODE_ENV=development
APP_NAME=KNOUX
# ... الإعدادات الأساسية
EOF
```

## 🏗️ استعادة من مشاكل التثبيت

### الحالة 1: فشل التثبيت على Windows
```powershell
# الخطوة 1: إلغاء التثبيت القديم
Get-AppxPackage *knoux* | Remove-AppxPackage

# الخطوة 2: تنظيف السجلات
Remove-Item -Path "$env:LOCALAPPDATA\KNOUX" -Recurse -Force
Remove-Item -Path "$env:APPDATA\KNOUX" -Recurse -Force

# الخطوة 3: تثبيت جديد
.\KNOUX_Setup.exe --install --force
```

### الحالة 2: مشاكل الأذونات
```bash
# فحص الأذونات
npm run check:permissions

# إصلاح الأذونات
npm run fix:permissions

# تثبيت مع صلاحيات
sudo npm run install:system
```

### الحالة 3: تعارض مع برامج أخرى
```bash
# الكشف عن التعارضات
npm run detect:conflicts

# إنشاء تقرير النظام
npm run system:report

# تشغيل في وضع العزل
npm start -- --sandbox
```

## ⚡ استعادة من أخطاء التشغيل

### الحالة 1: التطبيق لا يفتح
```bash
# الخطوة 1: تشغيل في وضع التصحيح
npm start -- --debug

# الخطوة 2: فتح أدوات المطور
npm start -- --dev-tools

# الخطوة 3: تشغيل بدون إضافات
npm start -- --no-extensions
```

### الحالة 2: أعطال متكررة
```bash
# الخطوة 1: جمع معلومات العطل
npm run collect:crash-report

# الخطوة 2: تحليل ملف الـ dump
npm run analyze:dump

# الخطوة 3: تشغيل في وضع الأمان
npm start -- --safe-mode
```

### الحالة 3: بطء الأداء
```bash
# الخطوة 1: مراقبة الموارد
npm run monitor:resources

# الخطوة 2: تنظيف الذاكرة المؤقتة
npm run clear:cache

# الخطوة 3: تحسين الإعدادات
npm run optimize:settings
```

## 💾 استعادة من فقدان البيانات

### الحالة 1: قاعدة بيانات تالفة
```bash
# الخطوة 1: التحقق من قاعدة البيانات
npm run db:check

# الخطوة 2: استعادة من النسخة الاحتياطية
npm run db:restore -- latest

# الخطوة 3: إصلاح قاعدة البيانات
npm run db:repair
```

### الحالة 2: فقدان تاريخ الحافظة
```bash
# الخطوة 1: البحث عن النسخ الاحتياطية
npm run backup:list

# الخطوة 2: استعادة نسخة محددة
npm run backup:restore -- 2024-01-15

# الخطوة 3: دمج البيانات
npm run data:merge
```

### الحالة 3: إعدادات مفقودة
```bash
# الخطوة 1: استعادة الإعدادات الافتراضية
npm run settings:reset

# الخطوة 2: استيراد الإعدادات المحفوظة
npm run settings:import -- backup/settings.json

# الخطوة 3: تزامن الإعدادات
npm run settings:sync
```

## 🔐 استعادة من مشاكل الأمان

### الحالة 1: فقدان مفاتيح التشفير
```bash
# الخطوة 1: إنشاء مفاتيح جديدة
npm run security:generate-keys

# الخطوة 2: إعادة تشفير البيانات
npm run security:reencrypt

# الخطوة 3: التحقق من السلامة
npm run security:verify
```

### الحالة 2: اختراق محتمل
```bash
# الخطوة 1: عزل النظام
npm run security:isolate

# الخطوة 2: فحص الثغرات
npm run security:scan

# الخطوة 3: إعادة التثبيت النظيف
npm run security:clean-install
```

### الحالة 3: بيانات مشفرة لا يمكن فتحها
```bash
# الخطوة 1: محاولة استعادة المفتاح
npm run security:recover-key

# الخطوة 2: استخدام النسخ الاحتياطية
npm run backup:decrypt

# الخطوة 3: استشارة خبير الأمان
npm run security:escalate
```

## 🛠️ أدوات الاستعادة

### أداة التشخيص (doctor.js)
```javascript
// تشخيص شامل للنظام
module.exports = {
  checkDependencies: () => { /* ... */ },
  checkFilesystem: () => { /* ... */ },
  checkDatabase: () => { /* ... */ },
  checkSecurity: () => { /* ... */ },
  generateReport: () => { /* ... */ }
};
```

### أداة الاستعادة (recovery.js)
```javascript
// استعادة تلقائية
module.exports = {
  recoverFromBuildFailure: () => { /* ... */ },
  recoverFromCrash: () => { /* ... */ },
  recoverFromDataLoss: () => { /* ... */ },
  recoverFromCorruption: () => { /* ... */ }
};
```

### أداة النسخ الاحتياطي (backup.js)
```javascript
// إدارة النسخ الاحتياطية
module.exports = {
  createBackup: () => { /* ... */ },
  restoreBackup: (backupId) => { /* ... */ },
  listBackups: () => { /* ... */ },
  cleanupOldBackups: () => { /* ... */ }
};
```

## 📊 مراقبة النظام والإنذارات

### مؤشرات الصحة
```typescript
interface SystemHealth {
  // النظام
  diskSpace: number;          // > 10%
  memoryUsage: number;        // < 80%
  cpuUsage: number;          // < 70%
  
  // التطبيق
  uptime: number;            // > 99%
  errorRate: number;         // < 0.1%
  responseTime: number;      // < 100ms
  
  // البيانات
  databaseHealth: number;    // 100%
  backupFreshness: number;   // < 24h
  encryptionStatus: boolean; // true
}
```

### إنذارات النظام
```bash
# إنذارات تلقائية
npm run monitor:alerts

# التحقق اليدوي
npm run check:alerts

# معالجة الإنذارات
npm run handle:alert -- <alert-id>
```

## 🔄 النسخ الاحتياطي والاستعادة

### جدولة النسخ الاحتياطي
```bash
# نسخ احتياطي يومي
npm run backup:daily

# نسخ احتياطي أسبوعي
npm run backup:weekly

# نسخ احتياطي يدوي
npm run backup:manual
```

### استعادة من النسخ الاحتياطي
```bash
# قائمة النسخ الاحتياطية
npm run backup:list

# استعادة آخر نسخة
npm run backup:restore -- latest

# استعادة نسخة محددة
npm run backup:restore -- 2024-01-15_10-30-00

# التحقق من الاستعادة
npm run backup:verify
```

### إدارة النسخ الاحتياطية
```bash
# تنظيف النسخ القديمة
npm run backup:cleanup

# نقل النسخ الاحتياطية
npm run backup:migrate

# تشفير النسخ الاحتياطية
npm run backup:encrypt
```

## 🧪 اختبارات الاستعادة

### اختبارات دورية
```bash
# اختبار استعادة البيانات
npm run test:recovery -- data

# اختبار استعادة النظام
npm run test:recovery -- system

# اختبار استعادة كاملة
npm run test:recovery -- full
```

### سيناريوهات الاختبار
1. **فشل القرص الصلب**: استعادة من نسخة خارجية
2. **فقدان البيانات**: استعادة من النسخ الاحتياطية
3. **تلف النظام**: إعادة تثبيت نظيف
4. **هجوم أمني**: استعادة نظيفة مع حماية

## 📝 توثيق عمليات الاستعادة

### سجل الاستعادة
```markdown
# RECOVERY_LOG.md
## استعادة في 2024-01-15
### المشكلة
فقدان بيانات تاريخ الحافظة بسبب عطل في قاعدة البيانات

### الإجراءات
1. تشخيص المشكلة: npm run db:check
2. استعادة النسخة الاحتياطية: npm run backup:restore latest
3. التحقق من السلامة: npm run db:verify

### النتائج
- البيانات المستعادة: 95%
- الوقت المستغرق: 15 دقيقة
- الخسائر: 5% من البيانات الحديثة
```

### تقارير ما بعد الاستعادة
```bash
# إنشاء تقرير الاستعادة
npm run report:recovery

# تحليل تأثير الاستعادة
npm run analyze:recovery-impact

# تحديث الوثائق
npm run docs:update-recovery
```

## 🎯 أفضل الممارسات للوقاية

### 1. النسخ الاحتياطي المنتظم
```bash
# تكوين النسخ الاحتياطي التلقائي
npm run config:auto-backup

# اختبار النسخ الاحتياطي
npm run test:backup

# مراجعة سياسات النسخ الاحتياطي
npm run review:backup-policy
```

### 2. المراقبة المستمرة
```bash
# مراقبة صحة النظام
npm run monitor:health

# إنذارات تلقائية
npm run setup:alerts

# تقارير دورية
npm run generate:reports
```

### 3. اختبارات الاستعادة
```bash
# جدولة اختبارات الاستعادة
npm run schedule:recovery-tests

# محاكاة الكوارث
npm run simulate:disaster

# تقييم الجاهزية
npm run assess:readiness
```

### 4. التوثيق الشامل
```bash
# تحديث أدلة الاستعادة
npm run docs:update-recovery-guide

# توثيق الحالات الخاصة
npm run docs:document-cases

# مشاركة المعرفة
npm run knowledge:share
```

## 📞 الدعم والمساعدة

### قنوات الدعم الفني
```bash
# دعم فوري (للأعطال الحرجة)
npm run support:emergency

# دعم تقني (للمشاكل المعقدة)
npm run support:technical

# استشارات (للوقاية والتخطيط)
npm run support:consultation
```

### موارد المساعدة
1. **الوثائق**: `docs/RECOVERY_GUIDE.md`
2. **الفيديو**: فيديوهات تعليمية للاستعادة
3. **المجتمع**: منتديات المساعدة
4. **الخبراء**: استشارات متخصصة

### تصعيد المشاكل
```bash
# تصعيد لمطوري النظام
npm run escalate:developers

# تصعيد لخبراء الأمن
npm run escalate:security

# تصعيد لإدارة النظام
npm run escalate:management
```

---

*دليل الاستعادة - الإصدار 1.0*  
*آخر تحديث: $(Get-Date -Format "yyyy-MM-dd")*

[البناء](./build-flow.md) | [التعبئة](./packaging-flow.md) | [الأمان](../docs/SECURITY.md)
